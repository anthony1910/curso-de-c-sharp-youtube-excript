using System;

namespace Variaveis
{
    class Program
    {
        static void Main(string[] args)
        {
            // Variável é um local na memória que reservamos para armazenar um valor
            int num = 200;// Não é possível alterar o tipo da variável durante o código
            Console.WriteLine(num);

            bool b = true;

            string s = "Aqui eu posso escrever textos";

            Console.WriteLine(b);
            Console.WriteLine(s);
        }
    }
}
