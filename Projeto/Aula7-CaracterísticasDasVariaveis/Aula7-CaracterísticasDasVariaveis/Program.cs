using System;

namespace VariaveisIII
{
    class Program
    {
        static void Main(string[] args)
        {
            // Características das Variáveis
            /*
             * 1) Nome
             * 2) Tipo
             * 3) Tamanho
             * 4) Valor
             * Colocar na memória apenas o que vamos utilizar
             * 
             * 1) Armazenamento é temporário
             * 2) Os dados sempre serão removidos da memória
             * 3) Os dados contidos nas variávies são voláteis
             * 4) O valor contido em uma variável é sobrescrito pelo novo valor
             * 5) O C# desaloca a memória automaticamente
            */

            int numeroTotal;// Variáveis não podem conter espaços. Primeiro nome minúsculo e o segundo é maiúsculo, por convensão. Não pode começar com número. Colocar _ se preciso
        }
    }
}
